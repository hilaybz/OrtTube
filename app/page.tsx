// Placeholder home page for the backend-rebuild phase.
// The real frontend (dashboard, student player, auth screens) is rebuilt in the
// dedicated frontend pass against the v2 schema.
export default function Home() {
  return (
    <main style={{ display: "grid", placeItems: "center", minHeight: "100vh" }}>
      <p>OrtTube — backend rebuild in progress.</p>
    </main>
  );
}
